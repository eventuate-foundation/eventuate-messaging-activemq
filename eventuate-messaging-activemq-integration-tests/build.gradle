apply plugin: PrivateModulePlugin

dependencies {
    compile project(":eventuate-messaging-activemq-consumer")
    compile project(":eventuate-messaging-activemq-producer")
    testCompile "org.springframework.boot:spring-boot-starter-test:$springBootVersion"
    compile "io.eventuate.util:eventuate-util-test:$eventuateUtilVersion"
}

test {
    forkEvery 1
}